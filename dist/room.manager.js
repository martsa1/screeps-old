var createJobsList = function() {
  // Get a reference to the room we are working in
  // var room = this

  // console.log('Room Object:', JSON.stringify(room.controller))
}

module.exports = createJobsList
